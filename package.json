{
  "name": "mu-pixel-realm",
  "version": "1.0.0",
  "description": "MMORPG pixel art inspirado en MU Online para Android + LAN",
  "scripts": {
    "build": "node build.js",
    "server": "node server/index.js",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "express": "^4.18.2",
    "socket.io": "^4.6.1",
    "socket.io-client": "^4.6.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
